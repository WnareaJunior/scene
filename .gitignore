# Dependencies
node_modules/
.npm

# Environment variables
.env
.env.local
.env.development
.env.staging
.env.production

# Database (dumps stay out; tracked migrations are the exception)
*.sql
*.dump
!backend/migrations/*.sql
!backend/migrations/legacy/*.sql

# React Native
.expo/
dist/
build/
ios/
android/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Coverage
coverage/
.nyc_output

# Claude Code worktrees — scratch checkouts, never part of the repo.
# One was committed as a bare gitlink (no .gitmodules), which made
# actions/checkout fail cleanup on every PR.
.claude/worktrees/
